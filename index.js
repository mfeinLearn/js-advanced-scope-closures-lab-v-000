function createDriver() {
  let driverId = 0;
  // return the class
  return class {
    constructor(name) {
      this.name = name;
      this.blockRange = blockRange;
      this.id = ++driverId;
    }

    produceDrivingRange(blockRange) {
      return foo;
      differenceFromRestToHome = restorantStreet - customerStreet;
      // the difference between the restorant and the home
      if (differenceFromRestToHome > this.blockRange) {
        return `${differenceFromRestToHome} blocks out of range`
          } else {
        return `within range by ${differenceFromRestToHome}`
      }// end of condition



    }
  };
}

const Driver = createDriver();
